import { Grid, Typography, Box } from "@mui/material";
import { styled } from "@mui/system";

export const OuterBoxContainer = styled(Box)`
  // background: ${({ theme }) => theme.palette.primaryWhite.main};
  margin-bottom: 10px;
  padding: 1rem;
`;
