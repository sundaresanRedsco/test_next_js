import GlobalIntegrations from "@/app/apiflow_Pages/settings/Integrations";
import React from "react";

type Props = {};

export default function AccountSettingsPage({}: Props) {
  return <GlobalIntegrations />;
}
